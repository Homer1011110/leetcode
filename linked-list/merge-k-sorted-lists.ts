import { ListNode } from "./data";



/*
 * 前所有队列的队头元素开始，进行排序
 * 选取最小的一个放到结果链表尾部，并将其所在队列的指针，移到下一个元素
 * 将新的队头列队排序，选出最小的一个xxxx
*/
function mergeKLists(lists: Array<ListNode | null>): ListNode | null {
  
  return null;
};